phpとstreamlitのインストールが必要
エディタ上で実行後、ターミナルで、 streamlit　run app.py　を実行
スライドで、集中する時間と休憩する時間を設定し、「FOCUS START」を押す。
「STOP」を押すまで集中と休憩がループし、終了後に履歴が表示される

タイマーと音を連動させたり、AIを使って、集中できたか、やもっと集中時間を延ばすか等の
フィードバックができたらよかった（リングフィットの負荷強度みたいな